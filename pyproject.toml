[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = [
    "migrations/versions",
]

[tool.ruff.lint]
select = ["F", "B"]
ignore = [
    "B008",
    "B904",
]

[tool.ruff.lint.isort]
known-first-party = ["app", "dashboard"]
